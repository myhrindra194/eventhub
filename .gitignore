# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
*.iml
.idea/
.vscode/settings.json
pubspec_overrides.yaml

# Generated code is committed on purpose for CI reproducibility? No: regenerate.
# Uncomment the two lines below if the team decides NOT to commit generated files.
# *.g.dart
# *.freezed.dart

# Firebase (secrets & local config)
lib/firebase_options.dart
android/app/google-services.json
ios/Runner/GoogleService-Info.plist
macos/Runner/GoogleService-Info.plist
.firebase/
firebase-debug.log
*.env
.env.*
!supabase/functions/.env.example

# OS
.DS_Store
Thumbs.db

# Android
android/.gradle/
android/local.properties
android/key.properties
**/*.keystore
**/*.jks

# iOS
ios/Pods/
ios/.symlinks/
ios/Flutter/ephemeral/
**/Flutter/ephemeral/

# Coverage
coverage/

# Local documents
*.pdf
*.jpeg
*.jpg
!assets/**

# Local credential files (FCM service account, SMTP, OAuth) — never committed
secrets/

# Build-time keys per flavor (Supabase URL/anon key, Google client id)
env/*.json
!env/example.json

# Supabase: database test suite dependencies, local function secrets
supabase/tests/node_modules/
supabase/functions/.env

# Firebase CLI (Hosting deploys)
firebase-debug.log

# Claude Code local worktrees
.claude/worktrees/

# Artefacts locaux de l'outil de design (hors projet Flutter)
/uploads/
/screenshots/
/.thumbnail
/support.js
