<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ remplace windowBackground par son propre écran de
         démarrage. On lui donne la couleur du canvas et une icône vide :
         le système affiche un aplat, puis le splash Flutter anime la marque
         sur le même fond, sans flash ni double logo. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowSplashScreenBackground">@color/launch_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon_blank</item>
        <item name="android:windowSplashScreenAnimationDuration">0</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@color/launch_background</item>
    </style>
</resources>
