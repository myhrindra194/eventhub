# EventHub — Architecture

Feature-first, clean-layered Flutter application backed entirely by Firebase.
The French, product-level overview is the root [`README.md`](../README.md);
this document is the technical reference.

```
lib/
├── main*.dart                  entrypoints (flavors)
├── bootstrap.dart              Firebase init, App Check, Crashlytics sink, FCM background handler
├── firebase_options.dart       generated by flutterfire (git-ignored)
├── app/
│   ├── app.dart                MaterialApp.router; starts push, analytics identity/consent, offline band
│   └── theme/                  "Aurora" tokens, typography, motion, ThemeData
├── routes/                     AppRoutes, RouteGuard (pure), router, observer → analytics screen views
├── core/
│   ├── analytics/              AppAnalytics (never throws, lazy SDK), AnalyticsConsent (opt-in, prefs)
│   ├── connectivity/           isOnline (connectivity_plus)
│   ├── config/                 Flavor, AppConfig (dart-defines), Clock, AppLinks
│   ├── errors/ · result/       Failure hierarchy, ErrorMapper (Auth, Firestore, Functions), Result, guard
│   ├── firebase/               SDK providers (Auth, Firestore + cache, Storage, Messaging, Functions), paths
│   ├── l10n/ · utils/          AppStrings, AppLogger (reporter hook), formats, validators
│   └── widgets/                design system, OfflineAware, consent sheet
└── features/
    ├── onboarding/
    ├── auth/                   email, Google, verification, profile, settings, account deletion
    ├── events/                 catalogue (live page + cursor pages), search, detail, form, share
    ├── reservations/           transactional booking, wallet, ticket
    ├── favorites/              users/{uid}/favorites
    ├── waitlist/               events/{id}/waitlist
    ├── reviews/                reviews/{eventId_uid}
    ├── checkin/                door scanning: TicketPayload, CheckInPolicy, events/{id}/checkins
    ├── organizer/              dashboard, stats, alerts, participants, CSV (file + clipboard)
    ├── organizers/             public organizer profile (organizers/{id}), follows
    ├── moderation/             reports: ReportPolicy, repository, report sheet
    ├── admin/                  moderation queue, file, decisions, admin roles (claim `admin`)
    ├── team/                   co-organizers: invitations, team screen, TeamPolicy
    ├── notifications/          preferences, devices, FCM pipeline, notification centre
    ├── participant/            participant shell, "Pour vous" recommendations
    └── support/                help, privacy, about

functions/                      Cloud Functions (TypeScript, 2nd gen) + integration tests
firebase/                       security rules, indexes, rules tests
hosting/public/                 landing page, 404, .well-known/assetlinks.json
```

## Dependency rule

```
presentation ──▶ application ──▶ domain ◀── data
```

* `domain` is pure Dart: entities, policies, calculators, repository
  interfaces. No Flutter, no Firebase.
* `data` implements the interfaces and is the only feature layer importing
  Firebase SDKs. `core/analytics` and `core/firebase` are infrastructure.
* `application` composes repositories into providers and controllers
  (`Future<Result<T>>` + in-flight `AsyncValue`). Controllers also emit
  analytics events on success.
* A feature never imports another feature's `data` layer; it composes its
  providers instead (check-in reads reservations through
  `reservationRepositoryProvider`, waitlist reads `myReservationForEventProvider`).

There is no simulated backend. Local development uses the emulator suite
(`USE_EMULATORS=true`). `bootstrap()` shows an error screen when Firebase
cannot be initialised.

## Error handling

Repositories never throw; `guard()` maps exceptions through `ErrorMapper`:

* `FirebaseAuthException` → `AuthFailure` (including `cancelled` for a closed
  Google picker, `accountExistsWithDifferentCredential`, `requiresRecentLogin`);
* callable functions (`plugin == firebase_functions`) → the server's French
  `HttpsError` message, `failed-precondition` as a `BusinessRuleFailure`;
* Firestore/Storage codes → `PermissionFailure`, `NetworkFailure`,
  `NotFoundFailure`, `StorageFailure`, `UnexpectedFailure`.

`AppLogger.error` forwards to `AppLogger.reporter` (Crashlytics on mobile).

## Domain rules

| Rule | Where |
|---|---|
| Booking: one per participant, not full, not started | `ReservationPolicy` (in transaction) + rules |
| Re-booking after cancellation | rules: recent `reservedAt`, cleared `cancelledAt` |
| Publishing requires a verified email | `EventFormController` + rules `isVerified()` |
| Deleting an event requires no seats taken | `EventPolicy.canDelete` + rules |
| Capacity never below seats taken | `EventPolicy.availablePlacesAfterCapacityChange` + rules |
| Waitlist: participant, sold out, upcoming, no seat | `WaitlistPolicy` + rules |
| Review: attendee, started, verified, 1–5, ≤ 2000 | `ReviewPolicy` + rules |
| Door admission | `CheckInPolicy` (pure) + append-only rules |
| Account deletion cascade | `deleteAccount` Cloud Function |
| Follow: not oneself; counter server-side | `FollowPolicy` + rules + `onFollowWritten` |
| Report: closed reasons, one per account, not one's own content | `ReportPolicy` + rules (deterministic id) |
| Review hidden after 3 distinct reports, or by an admin | `onReportCreated`, `moderateContent`; filtered client-side |
| Team: owner or co-organizer manages; only the owner deletes and composes the team | `EventPolicy`, `TeamPolicy` + rules `isEventTeam()` + team callables |

### Co-organizers

`Event.staffIds` lists co-organizers; `isManagedBy(uid)` = owner or staff.
The guest list uses two queries on purpose: the owner's pins
`organizerId ==` (no extra rule read), a co-organizer's pins `eventId ==` so
the rules can read that event and check `staffIds`
(`eventParticipantsProvider` picks one from the loaded event). The dashboard
merges `organizerEventsProvider` and `coOrganizedEventsProvider`
(`staffIds array-contains`). Invitations exist twice — under the event for
the team, under the invitee for their inbox — and every change goes through
`inviteCoOrganizer`, `respondToStaffInvite` or `removeCoOrganizer`.

Pure calculators: `Reservation.ticketCode/ticketPayload`, `TicketPayload.parse`,
`GuestListCsv` (+ `fileBytes`, `fileNameFor`), `OrganizerStats`,
`OrganizerAlerts`, `ReviewSummary`, `mergeCatalogue`, `NotificationRoute`,
`NotificationPreferences`, `Attendance.sentence`, `Recommender.rank`,
`OrganizerProfile.averageRating`.

## Authentication

* `watchSession()` is built on `userChanges()` (so an email-verification reload
  reaches the UI), `distinct` on uid + `emailVerified`, `switchMap` onto the
  Firestore profile, with a grace period before `ProfileMissing`.
* `AppUser.emailVerified` comes from Firebase Auth, never from Firestore.
* Google Sign-In 7: native picker → ID token → `GoogleAuthProvider.credential`;
  web: `signInWithPopup`. A first Google sign-in has no profile →
  `ProfileMissing(displayName)` → role choice on `/complete-profile`.
* After the profile appears, the repository waits for the `role` custom claim
  (`setRoleClaim`) and refreshes the token.
* Sign-up sends the verification email; `refreshEmailVerification()` reloads
  and force-refreshes the token.
* Account deletion: re-authenticate (password or Google), call the
  `deleteAccount` callable, sign out.

## Navigation

Two `StatefulShellRoute`s (participant 4 tabs, organizer 4 tabs). Leaf routes
(detail, ticket, favorites, forms, participants, check-in, notification centre,
support pages) sit on the root navigator. `RouteGuard.redirect` is a pure
function; role-agnostic pages (`/welcome`, `/change-password`,
`/account/edit`, `/notifications`, `/help`, `/privacy`, `/about`,
`/following`, and every `/organizers/{id}`) are declared in `AppRoutes`. The
router observer reports screen views to analytics.

### Administration

`/admin/**` is outside both role areas. `RouteGuard` lets it through only for
`AppUser.isAdmin`, which `AuthRepositoryImpl` reads from the ID token claims
(`FirebaseAuthDataSource.hasAdminClaim`). The admin feature composes other
features' providers for previews (`eventByIdProvider`,
`reviewRepositoryProvider`, `organizerProfileProvider`) and owns only the
queue, reports, decisions, reported account and admin list reads, plus the
`moderateContent` / `setAdminRole` callables. `ModerationPolicy.actionsFor`
mirrors `ACTIONS_BY_TARGET` on the server.

### Deep links

`https://eventhub-d411f.web.app/e/{id}` is one URL for everyone:

* **App installed (Android)** — the manifest declares an auto-verified intent
  filter on `/e/`; Android checks `hosting/public/.well-known/assetlinks.json`
  and hands the URL to the app. Route `/e/:eventId` redirects to
  `/events/:eventId`.
* **No app / other platform** — Hosting rewrites `/e/**` to the
  `publicEventPage` function: an HTML page with Open Graph and Twitter tags
  (link previews), escaped user content, no attendee data, cached 5–10 min.

A link usually cold-starts the app, i.e. arrives while booting or signed out.
`RouteGuard` carries it as `?from=` through `/splash` and `/login` and
resumes it once signed in, role confinement still applying. Only
`AppRoutes.isDeepLinkTarget` locations (`/e/`, `/events/`, `/organizers/`)
are honoured — `from` is external input.

## Catalogue pagination

Security rules cap a `list` at 100 documents. The catalogue is:

1. `upcomingEvents` — the first 100 upcoming events, **live**, ordered by
   `startsAt` then document id;
2. `CatalogueExtraPages` — older pages fetched on demand with
   `startAfter([startsAt, id])`, one-shot, reset by pull-to-refresh;
3. `catalogue` — `mergeCatalogue(live, older)`: deduplicated, live wins,
   catalogue order. Rails, search filters and category counts read it.

## Push notifications

**Server** (`functions/src/index.ts`): `notifyOrganizerOnReservation`
(booking/cancellation; also removes the booker's waitlist entry),
`sendEventReminders` (hourly, delegates to exported `runEventReminders`),
`notifyWaitlistOnSeatRelease` (seats freed → oldest waiting, one per seat,
`notifiedAt`). Each send reads `private/notifications`, writes
`users/{uid}/notifications` (TTL), pushes to `devices`, deletes stale tokens.

**Client** (`features/notifications/`): `PushNotifications` follows the
session (permission, token, refresh, foreground display, taps, `deleteToken`
on sign-out); web uses a VAPID key and `web/firebase-messaging-sw.js`.
`NotificationRoute.locationFor(data)` is the single routing function for
system notifications and the notification centre.

## Server-maintained data

| Data | Written by | Read by the app as |
|---|---|---|
| `organizers/{id}` (name, bio, counters) | `syncOrganizerProfile`, `onFollowWritten`, `onEventWritten`, `aggregateOrganizerRating` | `organizerProfileProvider(id)` |
| `organizers/{id}/followers/{uid}` | `onFollowWritten` (mirror of `users/{uid}/following`) | never (fan-out only) |
| `aggregates/event_{id}.recentAttendees` | `aggregateAttendance`, `deleteAccount` | `eventRecentAttendeesProvider(id)` |
| `reviews/{id}.hidden` | `onReportCreated` (threshold), `moderateContent` | filtered out of `eventReviews` |
| `moderationQueue/*`, `audit/*` | `onReportCreated`, `moderateContent`, `once()` markers | admins, console only |

Triggers are delivered at least once. Counter updates go through `once()`,
which reads `audit/fx_{eventId}` and writes it in the same transaction as the
counter; the mirror and aggregate updates are idempotent by construction.

## Discovery

* **Social proof (F-07)** — head count from `Event.reservedCount` (exact,
  transactional), short names from the aggregate; `Attendance.sentence` builds
  the French sentence.
* **Recommendations (F-18)** — `Recommender.rank` over the loaded catalogue,
  favourites, active bookings and followed organizers; computed on device,
  reason attached to each suggestion.

## Production concerns

* **App Check** — activated in `bootstrap()` (Play Integrity / App Attest in
  release, debug providers otherwise, reCAPTCHA v3 on web); callable functions
  honour `ENFORCE_APP_CHECK`.
* **Crashlytics** — fatal Flutter/platform errors and logged errors; collection
  off in debug; user id set from the session.
* **Analytics** — opt-in: collection disabled in the manifest/Info.plist,
  enabled by `AnalyticsConsent`; `AppAnalytics` never throws.
* **Offline** — Firestore persistence (100 MB cache); `OfflineAware` band.
* **Release signing** — `android/key.properties`.

## Data model

See README §6. Deterministic ids everywhere uniqueness matters; reservations
denormalise user and event fields; ticket codes are derived.

## Code generation

`freezed`, `json_serializable`, `riverpod_generator` — `make gen`. Generated
files are committed.

## Flavors and configuration

`FLAVOR`, `USE_EMULATORS`, `GOOGLE_SERVER_CLIENT_ID`, `FIREBASE_WEB_VAPID_KEY`,
`APP_CHECK_RECAPTCHA_SITE_KEY` (dart-defines); `AppConfig.functionsRegion`
must equal `REGION` in the functions — `us-central1`, because the `(default)`
Firestore database is in `nam5` and Firestore triggers must run in the
database's location.

`firebase.json` also carries the `flutter` block written by
`flutterfire configure` (app ids per platform), so `flutterfire configure`
without arguments regenerates `lib/firebase_options.dart` and
`android/app/google-services.json`, both git-ignored.

## Testing strategy

| Layer | How |
|---|---|
| domain | pure unit tests (policies, calculators, parsers, recommender, attendance sentence) |
| core | Result/guard, ErrorMapper, AppLogger |
| routes | RouteGuard as a pure function |
| presentation | widget + golden tests with provider overrides (auth screens, startup, ticket, stats, notification centre) |
| rules | `@firebase/rules-unit-testing` on emulators (`make test-rules`) |
| functions | `node:test` integration tests on Auth/Firestore/Functions/Storage emulators (`make test-functions`): triggers observed through their effects, callables and the public page over HTTP |
