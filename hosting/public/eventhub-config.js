// Public configuration of the site. Nothing secret lives here: these are the
// same identifiers as the `web` entry of lib/firebase_options.dart, shipped in
// every build of the app. What protects the data is firebase/firestore.rules
// (events allow a public `get`, nothing else is readable without an account).
// Keep in sync with `flutterfire configure` if the project changes.
window.EVENTHUB = {
  "projectId": "eventhub-d411f",
  "apiKey": "AIzaSyAHXChbD1xYXawt3EG8QNWZjVoM65xyW-4",
  "androidPackage": "com.example.eventhub"
};
